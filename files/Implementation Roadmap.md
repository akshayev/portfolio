Task 1: The Procedural Loader"Create a React component using Tailwind CSS for a minimalist loader. It should have a centered, pulsing geometric logo and a thin horizontal progress bar using hex #FF7582. The loader should disappear only when the R3F Canvas is fully mounted."Task 2: The R3F Camera Scroll"Set up a React Three Fiber scene where the camera position (z-axis) is tied to the window scroll percentage using GSAP ScrollTrigger. Create a 'Timeline' component that renders 3D text at specific z-intervals based on an array of project data."Task 3: Interactive Skills Node-Graph"Write a R3F component that generates 20 small spheres (nodes) at random positions within a 3D sphere. Use math to draw lines between them. When a node is hovered, it should glow hex #FF7582 and display a technical skill label like 'Python', 'Supabase', or 'Gemini 2.0' in bold sans-serif." Task 4: Dynamic Project Case Studies"Create a React Route for '/project/:id'. It should fetch data from Supabase for that project. The background should remain the kinetic 3D sunset, but the foreground should display a clean glassmorphism card with the project's technical architecture, a feature breakdown, and a prominent GitHub button." V. Final Polish: SFX & MobileSFX Logic: Use the use-sound hook. Trigger a "mechanical click" when the scroll hits a new timeline stop and a "glitch sweep" when entering a case study page.Mobile Optimization: In R3F, use the dpr (device pixel ratio) prop to downscale quality on mobile devices to maintain 60FPS. Ensure the massive typography scales down using clamp() in CSS.